<Window x:Class="SpaceHavenEditor2.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpaceHavenEditor2"
        mc:Ignorable="d"
        Title="Editor Settings" Height="250" Width="650"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="WhiteSmoke" WindowStyle="None"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Direction="270" Color="Gray" Opacity="0.3" BlurRadius="5"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Visibility" Value="Visible"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF009AFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="Gray"/>
                                <Setter Property="Foreground" Value="DarkGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Backup checkbox -->
                <RowDefinition Height="Auto"/>
                <!-- Default directory + Browse -->
                <RowDefinition Height="Auto"/>
                <!-- OK/Cancel buttons -->
            </Grid.RowDefinitions>

            <!-- Row 0: Backup Checkbox -->
            <CheckBox x:Name="chkBackup" Grid.Row="0" VerticalAlignment="Center" Margin="5,5,5,10">
                <TextBlock Text="Create timestamped backup when opening save file" FontWeight="Bold"/>
            </CheckBox>

            <!-- Row 1: Default Directory + Browse -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5,10,5,15">
                <TextBlock Text="Default Save Directory:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,15,0"/>
                <TextBox x:Name="txtDefaultDirectory" Width="300" VerticalAlignment="Center" IsReadOnly="True" 
                         Style="{StaticResource ModernTextBoxStyle}"/>
                <Button x:Name="btnBrowseDirectory" Content="Browse…" Margin="10,0,0,0" Background="Red" 
                        Style="{StaticResource ModernButtonStyle}" Click="btnBrowseDirectory_Click"/>
            </StackPanel>

            <!-- Row 2: OK/Cancel Buttons -->
            <Grid Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
                <UniformGrid Rows="1" Columns="2">
                    <Button x:Name="btnOK" Content="OK" IsDefault="True" Click="btnOK_Click" 
                            Style="{StaticResource ModernButtonStyle}" Margin="0,0,5,0"/>
                    <Button x:Name="btnCancel" Content="Cancel" IsCancel="True" Click="btnCancel_Click" 
                            Style="{StaticResource ModernButtonStyle}" Background="#FFAAAAAA" Margin="5,0,0,0"/>
                </UniformGrid>
            </Grid>
        </Grid>
    </Border>
</Window>